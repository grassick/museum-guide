# No environment variables required for production.
# The OpenRouter API key is configured at runtime via the Settings UI
# and stored in the browser's localStorage.
#
# For local development, no .env file is needed.
